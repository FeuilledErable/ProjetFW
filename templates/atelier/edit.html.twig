{% extends 'base.html.twig' %}

{% block title %}Edit Atelier{% endblock %}

{% block body %}
    <h1 class="text-uppercase nav justify-content-center"><u>Edit Atelier</u></h1>
    <br>

    <div class="card w-75 p-3 mx-auto border border-secondary bg-light">
        <div class="card-body bg-light" >

            {{ include('layers/_form.html.twig', {'button_label': 'Update'}) }}
            {{ include('layers/_delete_form.html.twig') }}
            <a href="{{ path('app_atelier_list') }}">Retourner à la liste</a>

            </div>
    </div>


{% endblock %}
