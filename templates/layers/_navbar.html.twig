<nav class="navbar navbar-dark bg-success">
    <!-- Navbar content -->
    <div class="container">
        <a class="navbar-brand mr-auto" href="{{ path('app_atelier_index') }}">
            <i class="bi-house" style="font-size: 2rem; color: black;"></i>
        </a>
        {% if is_granted('ROLE_INSTRUCTEUR') %}
            <a class="navbar-brand ml-auto" href="{{ path('app_atelier_new') }}">
                <i class="bi-plus" style="font-size: 2rem; color: black;"></i>
            </a>
        {% endif %}
        <a class="navbar-brand ml-auto" href="{{ path('app_atelier_list') }}">
            <i class="bi bi-list-ul" style="font-size: 2rem; color: black;"></i>
        </a>
        {% if app.user %}
            {% if is_granted('ROLE_INSTRUCTEUR') or is_granted('ROLE_APPRENTI') %}
                <a class ="navbar-brand ml-auto" href="{{ path('app_profile') }}">
                    <i class="bi bi-person-circle" style="font-size: 2rem; color: black;"></i>
                </a>
            {% endif %}
            <a class ="navbar-brand ml-auto" href="{{ path('app_logout') }}">
                <i class="bi-arrow-bar-right" style="font-size: 2rem; color: black;"></i>
            </a>

            {% if is_granted('ROLE_ADMIN')%}
                <a class ="navbar-brand ml-auto" href="{{ path('app_admin_index') }}">
                    <i class="bi bi-pencil-fill" style="font-size: 2rem; color: black;"></i>
                </a>
            {% endif %}
        {% else %}
            <a class ="navbar-brand ml-auto" href="{{ path('app_login') }}">
                <i class="bi-arrow-bar-left" style="font-size: 2rem; color: black;"></i>
            </a>
        {% endif %}
    </div>
</nav>